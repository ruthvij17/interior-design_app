const connection = require("../Utils/SQLconnection");

// const deleteWorker = (req, res, next) => {
//   console.log("del worker");
//   let w_id = req.params.id;
//   if (!w_id) return res.status(409).json({ msg: "bad request error" });
//   try{
//     let q="delete from worker where w_id"
//   }
//   console.log(w_id);
// };

module.exports = {};
